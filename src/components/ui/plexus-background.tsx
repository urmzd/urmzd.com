'use client';

// Re-export the WebGL implementation as the default PlexusBackground.
// Import path stays identical so LandingExperience.tsx needs no changes.
export { PlexusBackground } from './plexus-webgl/PlexusScene';
